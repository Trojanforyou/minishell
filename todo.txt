int find_redirect_index(char **av, int *type)
{
    // Найти позицию ">" в массиве аргументов
    // Определить тип редиректа
    // Вернуть индекс
}
t_redir *create_redirect(int type, char *filename)
{
    // Выделить память
    // Заполнить поля
    // Вернуть структуру
}


int execute_redirect(t_redir *redir)
{
    // Открыть файл с нужными флагами
    // Сделать dup2()
    // Сохранить оригинальный fd для восстановления
}

char **remove_redirects(char **av)
{
    // Создать новый массив без ">" и имени файла
    // Вернуть "чистые" аргументы для execve
}
